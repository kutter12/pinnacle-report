services:
  - type: web
    name: pinnacle-report
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ADMIN_KEY
        value: your-admin-key-here
